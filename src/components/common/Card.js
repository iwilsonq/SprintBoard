import React from 'react';
import CardHeader from './CardHeader';
import CardSection from './CardSection';

const Card = props => {
  return (
    <div className="card">
      <CardHeader />
      a card
      <CardSection />
    </div>
  );
};

export { Card };
